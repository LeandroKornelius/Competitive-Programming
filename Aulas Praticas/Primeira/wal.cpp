#include<bits/stdc++.h>
using namespace std;

#define ll long long

int main() {
    std::ios::sync_with_stdio(false);
    cin.tie(NULL);

    int t;
    vector<int> nums;
    cin >> t;

    for (int i = 0; i < t; i++) {
        int aux;
        cin >> aux;
        nums.push_back(aux);
        int soma = nums[nums.size() - 1];
        int ind = 0;

    }
    
    return 0;
}