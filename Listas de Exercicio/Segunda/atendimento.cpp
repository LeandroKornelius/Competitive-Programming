#include<bits/stdc++.h>
using namespace std;

int main() {
    std::ios::sync_with_stdio(false);
    cin.tie(NULL);

    int n, m, tempo, itens;

    cin >> n >> m;

    for (int i = 0; i < m; i++) {
        
    }

    return 0;
}